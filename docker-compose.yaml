services:
  mosquitto:
    image: eclipse-mosquitto:1.6
    volumes:
      - ./config/:/mosquitto/config/:ro
      - ./log/:/mosquito/log/
      - data:/mosquito/data/
    ports:
      - 1883:1883
      - 9001:9001

volumes:
  data: ~
